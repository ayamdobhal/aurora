const {test}=require('node:test');const assert=require('node:assert/strict');const {fixture}=require('./helpers/runtime.cjs');const {buildSync}=require('esbuild');
const code=buildSync({stdin:{contents:'import {setupFocusMode} from "./src/lib/focus-mode";setupFocusMode();',resolveDir:process.cwd()},bundle:true,write:false,format:'iife'}).outputFiles[0].text;
test('focus mode restores route and focus, and never persists transient state',()=>{const f=fixture('<button id="origin">Focus</button>');try{f.w.Spicetify.Platform.History.location={pathname:'/album/example'};const origin=f.w.document.querySelector('#origin');origin.focus();f.w.eval(code);f.w.document.dispatchEvent(new f.w.Event('toggle-focus'));assert.ok(f.w.document.body.classList.contains('aurora-focus'));assert.deepEqual(f.calls[0],['navigate','/lyrics']);f.key('Escape');assert.equal(f.w.document.body.classList.contains('aurora-focus'),false);assert.deepEqual(f.calls[1],['navigate','/album/example']);assert.equal(f.w.document.activeElement,origin);assert.equal(f.w.localStorage.length,0);}finally{f.dom.window.close()}});
